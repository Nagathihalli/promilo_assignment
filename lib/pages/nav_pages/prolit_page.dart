import 'package:flutter/material.dart';

class ProletPage extends StatelessWidget {
  const ProletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Prolet Page'),
    );
  }
}
